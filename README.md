# capstone-web-dev

This is a transport tracker application built with React and Vite.

## Features

- Map view for tracking routes
- Home page
- Route services

## Getting Started

1. Install dependencies: `npm install`
2. Run the development server: `npm run dev`

## Build

`npm run build`
